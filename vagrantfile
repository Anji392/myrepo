Vagrant.configure("2") do |config|
  # existing config...

  # Forward Flask app port from guest (VM) to host (Windows)
  config.vm.network "forwarded_port", guest: 7000, host: 7000, auto_correct: true
end

